// Introdução ao Array
console.log(aTemps[0])

var daysOfWeek = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday']

for (day in daysOfWeek) {
    console.log(daysOfWeek[day])
}
